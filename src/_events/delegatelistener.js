/*export default function(object, name, path, callback, context) {
	const key = path.pop(),
		info = d;//.get
	if(key) {
		onChange(object, key, function(evt) {
			undelegateListener(evt.previousValue, name, path, callback, context);
			delegateListener(evt.value, name, path, callback, context);
		});
	} else {
		addListener(object, name, callback, context);
	}
}
*/

export default 5;
